# إضافة ميزة رفع الوسائط في المجتمع

## ملخص التحديثات

تم إضافة إمكانية رفع الصور والفيديوهات والصوتيات في قسم المجتمع (Community) على الموقع، بشكل مشابه لتطبيق الموبايل.

## الملفات المعدلة

### 1. `templates/community_bonus.html`

#### الإضافات في HTML:
- أضفنا قسم رفع الوسائط تحت منطقة كتابة المنشور
- أزرار لرفع: صور (متعددة) - فيديو - صوت
- منطقة معاينة للملفات المرفوعة قبل النشر
- زر حذف لكل ملف

#### الإضافات في CSS:
```css
/* Media Upload Section */
.media-upload-section
.media-upload-buttons
.media-btn
.media-preview-container
.media-preview-item
.media-preview-remove
```

#### الإضافات في JavaScript:
**متغيرات عامة:**
```javascript
let uploadedImages = [];      // مصفوفة للصور (حتى 10 صور)
let uploadedVideo = null;      // فيديو واحد
let uploadedAudio = null;      // ملف صوتي واحد
```

**الدوال الجديدة:**
- `handleImageUpload(event)` - معالجة رفع الصور (متعددة)
- `handleVideoUpload(event)` - معالجة رفع الفيديو (واحد)
- `handleAudioUpload(event)` - معالجة رفع الصوت (واحد)
- `updateMediaPreview()` - تحديث معاينة الملفات المرفوعة
- `removeMedia(type, index)` - حذف ملف من القائمة
- تحديث `createPost()` - إضافة الملفات إلى FormData عند النشر
- تحديث `postMediaHtml()` - دعم عرض media_preview_urls
- تحديث `normalizePostsFromApi()` - تحليل JSON للملفات المرفوعة

### 2. `community_bonus_routes.py`

#### تحديث route `/community_bonus/create_post`:
- معالجة رفع الصور المتعددة من حقل `images`
- معالجة رفع الفيديو من حقل `video`
- معالجة رفع الصوت من حقل `audio`
- حفظ الملفات في: `static/uploads/community/`
- تسمية الملفات: `{user_id}_{timestamp}_{filename}`
- تخزين معلومات الملفات في قاعدة البيانات:
  - `media_file_ids` - أسماء الملفات
  - `media_types` - أنواع الملفات (image/video)
  - `media_preview_urls` - مسارات العرض
  - `audio_file_id` - اسم ملف الصوت
  - `audio_url` - مسار ملف الصوت

#### تحديث route `/community_bonus/get_posts`:
- إضافة `media_preview_urls` إلى استجابة JSON
- دعم عرض الملفات المرفوعة محلياً

### 3. `create_community_uploads_folder.py`

سكريبت لإنشاء مجلد التحميلات:
```
static/uploads/community/
```

## القواعد والحدود

### الصور (Images):
- ✅ يمكن رفع حتى 10 صور في منشور واحد
- ✅ الحد الأقصى لحجم كل صورة: 10MB
- ❌ لا يمكن رفع صور مع فيديو أو صوت

### الفيديو (Video):
- ✅ فيديو واحد فقط لكل منشور
- ✅ الحد الأقصى لحجم الفيديو: 50MB
- ❌ لا يمكن رفع فيديو مع صور أو صوت

### الصوت (Audio):
- ✅ ملف صوتي واحد فقط لكل منشور
- ✅ الحد الأقصى لحجم الملف: 20MB
- ❌ لا يمكن رفع صوت مع صور أو فيديو

### عام:
- ✅ يمكن نشر منشور بدون محتوى نصي إذا كان هناك ملف مرفق
- ✅ يمكن نشر منشور بمحتوى نصي فقط بدون ملفات
- ✅ المنشورات تدعم النشر المجهول مع الملفات

## التوافق مع التطبيق

الآن قسم المجتمع في الموقع يدعم:
- ✅ رفع صور متعددة
- ✅ رفع فيديو
- ✅ رفع صوت
- ✅ معاينة الملفات قبل النشر
- ✅ حذف الملفات قبل النشر
- ✅ عرض الوسائط في المنشورات
- ✅ نفس التجربة الموجودة في التطبيق

## هيكل قاعدة البيانات

جدول `community_post` يحتوي على:
```sql
media_file_ids TEXT       -- JSON array: ["file1.jpg", "file2.jpg"]
media_types TEXT          -- JSON array: ["image", "image"]
media_preview_urls TEXT   -- JSON array: ["/static/uploads/...", ...]
audio_file_id VARCHAR(255) -- اسم ملف الصوت
audio_url TEXT            -- مسار ملف الصوت
```

## الاختبار

لاختبار الميزة:
1. افتح `/community_bonus`
2. اضغط على أيقونة الصورة/فيديو/صوت
3. اختر ملف
4. شاهد المعاينة
5. انشر المنشور
6. تحقق من ظهور الوسائط في المنشور

## ملاحظات مهمة

1. **الأمان**: الملفات تُحفظ مع أسماء فريدة تتضمن معرف المستخدم والتاريخ
2. **التخزين**: الملفات تُحفظ في `static/uploads/community/`
3. **الأداء**: الصور تُحمل بشكل lazy loading
4. **المتصفحات**: يدعم جميع المتصفحات الحديثة
5. **الموبايل**: واجهة متجاوبة تدعم اللمس

## التطويرات المستقبلية المقترحة

- [ ] ضغط الصور تلقائياً قبل الرفع
- [ ] دعم تعديل المنشورات مع الوسائط
- [ ] إضافة تأثيرات/فلاتر على الصور
- [ ] دعم GIF
- [ ] معرض صور لعرض الصور بشكل أكبر
- [ ] تحميل تدريجي للفيديوهات الكبيرة

## المطور
تم التطوير بواسطة Kiro AI
التاريخ: 2026-07-14
